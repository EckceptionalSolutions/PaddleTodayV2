---
import FeatureIcon from '../components/FeatureIcon.astro';
import { listRiverStateGroups } from '../lib/rivers';

type Props = {
  title: string;
  description?: string;
  noindex?: boolean;
  structuredData?: Record<string, unknown> | Array<Record<string, unknown>>;
  socialTitle?: string;
  socialDescription?: string;
  socialImage?: string;
  socialImageAlt?: string;
  socialImageWidth?: number;
  socialImageHeight?: number;
  ogType?: string;
  twitterCard?: 'summary' | 'summary_large_image';
  bodyClass?: string;
  headerTitle?: string;
  headerBlurb?: string;
};

const {
  title,
  description = 'Live river scores, confidence, and explanations for paddlers.',
  noindex = false,
  structuredData,
  socialTitle = title,
  socialDescription = description,
  socialImage: socialImageProp,
  socialImageAlt = 'Paddle Today social preview',
  socialImageWidth,
  socialImageHeight,
  ogType = 'website',
  twitterCard = 'summary_large_image',
  bodyClass = '',
  headerTitle = 'Find the best paddle near you today',
  headerBlurb = 'Live score, water level, and weather.',
} =
  Astro.props as Props;
const canonicalUrl = Astro.site ? new URL(Astro.url.pathname, Astro.site).href : Astro.url.href;
const socialImage = socialImageProp
  ? Astro.site
    ? new URL(socialImageProp, Astro.site).href
    : socialImageProp
  : Astro.site
    ? new URL('/brand/paddle-today-share-card-v2-1200x630.png', Astro.site).href
    : undefined;
const siteUrl = Astro.site ? Astro.site.href : `${Astro.url.origin}/`;
const organizationId = new URL('/#organization', siteUrl).href;
const websiteId = new URL('/#website', siteUrl).href;
const defaultStructuredData = [
  {
    '@context': 'https://schema.org',
    '@type': 'Organization',
    '@id': organizationId,
    name: 'Paddle Today',
    url: new URL('/', siteUrl).href,
    logo: socialImage,
  },
  {
    '@context': 'https://schema.org',
    '@type': 'WebPage',
    '@id': `${canonicalUrl}#webpage`,
    name: title,
    description,
    url: canonicalUrl,
    isPartOf: {
      '@id': websiteId,
    },
    about: {
      '@id': organizationId,
    },
  },
];
const extraStructuredData = structuredData
  ? Array.isArray(structuredData)
    ? structuredData
    : [structuredData]
  : [];
const structuredDataJson = JSON.stringify([...defaultStructuredData, ...extraStructuredData]);
const footerStateLinks = listRiverStateGroups();
const generatedPageClass =
  Astro.url.pathname === '/'
    ? 'page-home'
    : `page-${Astro.url.pathname
        .replace(/^\/+|\/+$/g, '')
        .replace(/[^a-zA-Z0-9/_-]+/g, '')
        .replace(/[\\/]+/g, '-')
        .toLowerCase() || 'page'}`;
const pageClass = [generatedPageClass, bodyClass].filter(Boolean).join(' ');
const umamiWebsiteId =
  import.meta.env.PUBLIC_UMAMI_WEBSITE_ID || 'ce97ebc3-44ba-4c12-898e-666c904bc6b6';
const umamiScriptSrc = import.meta.env.PUBLIC_UMAMI_SCRIPT_SRC || 'https://cloud.umami.is/script.js';
const assetVersion = import.meta.env.PUBLIC_ASSET_VERSION || '20260812-ops-tabs2';
const appleAppId = import.meta.env.PUBLIC_APPLE_APP_ID || '6769542734';
const appStoreUrl = import.meta.env.PUBLIC_APP_STORE_URL || (appleAppId ? `https://apps.apple.com/app/id${appleAppId}` : '');
const playStoreUrl =
  import.meta.env.PUBLIC_PLAY_STORE_URL || 'https://play.google.com/store/apps/details?id=com.paddletoday.mobile';
const appDownloadConfigJson = JSON.stringify({
  appStoreUrl,
  playStoreUrl,
  appleAppId,
});
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="robots" content={noindex ? 'noindex, nofollow' : 'index, follow'} />
    <link rel="canonical" href={canonicalUrl} />
    <meta property="og:type" content={ogType} />
    <meta property="og:site_name" content="Paddle Today" />
    <meta property="og:title" content={socialTitle} />
    <meta property="og:description" content={socialDescription} />
    <meta property="og:url" content={canonicalUrl} />
    {socialImage && <meta property="og:image" content={socialImage} />}
    {socialImage && <meta property="og:image:alt" content={socialImageAlt} />}
    {socialImageWidth && <meta property="og:image:width" content={String(socialImageWidth)} />}
    {socialImageHeight && <meta property="og:image:height" content={String(socialImageHeight)} />}
    <meta name="twitter:card" content={twitterCard} />
    <meta name="twitter:title" content={socialTitle} />
    <meta name="twitter:description" content={socialDescription} />
    {socialImage && <meta name="twitter:image" content={socialImage} />}
    {socialImage && <meta name="twitter:image:alt" content={socialImageAlt} />}
    {
      appleAppId && (
        <meta
          name="apple-itunes-app"
          content={`app-id=${appleAppId}`}
        />
      )
    }
    {
      umamiWebsiteId && <script defer src={umamiScriptSrc} data-website-id={umamiWebsiteId}></script>
    }
    <script type="application/ld+json" set:html={structuredDataJson}></script>
    <link rel="icon" type="image/png" href="/brand/paddle-today-logo-transparent-512.png" />
    <link rel="shortcut icon" href="/brand/paddle-today-logo-transparent-512.png" />
    <link rel="apple-touch-icon" href="/brand/paddle-today-logo-transparent-512.png" />
    <link rel="stylesheet" href={`/global.css?v=${assetVersion}`} />
  </head>
  <body class={pageClass}>
    <div class="page-shell">
      <header class="site-header">
        <a class="brand" href="/">
          <picture>
            <source srcset="/brand/paddle-today-logo-180.webp" type="image/webp" />
            <img
              class="brand__logo"
              src="/brand/paddle-today-logo-transparent-512.png"
              alt="Paddle Today"
              width="180"
              height="180"
              decoding="async"
            />
          </picture>
          <div class="brand__copy brand__copy--with-wordmark">
            <span class="brand__eyebrow">Paddle Today</span>
            <span class="brand__title">{headerTitle}</span>
          </div>
        </a>

        <div class="site-header__meta">
          <div class="site-header__topline">
            <button
              class="site-header__search"
              type="button"
              data-site-search-open
              data-analytics-event="Open site search"
              aria-haspopup="dialog"
              aria-controls="site-search-panel"
            >
              <FeatureIcon name="search" size={15} />
              <span class="site-header__search-label site-header__search-label--full">Search Routes</span>
              <span class="site-header__search-label site-header__search-label--short">Search</span>
            </button>
            <nav class="site-header__nav" aria-label="Primary">
              <a class="site-header__nav-link site-header__nav-link--primary" href="/">Today</a>
              <a class="site-header__nav-link site-header__nav-link--primary" href="/weekend/">Weekend</a>
              <a class="site-header__nav-link site-header__nav-link--primary" href="/explore/">
                <span class="site-header__nav-label site-header__nav-label--full">Explore</span>
                <span class="site-header__nav-label site-header__nav-label--short">Explore</span>
              </a>
              <a class="site-header__nav-link site-header__nav-link--primary" href="/favorites/" data-site-favorites-link hidden>
                My Routes <span data-site-favorites-count></span>
              </a>
            </nav>
            <a class="site-header__action" href="/request-river/" data-analytics-event="Open route request">
              <span class="site-header__action-label site-header__action-label--full">Request</span>
              <span class="site-header__action-label site-header__action-label--short">Request</span>
            </a>
          </div>
          <p class="site-header__blurb">
            {headerBlurb}
          </p>
          <p class="site-header__freshness" data-home-freshness-wrap hidden>
            <span class="site-header__freshness-item">Data refreshes every 30 minutes.</span>
            <span class="site-header__freshness-item site-header__freshness-item--updated" data-home-freshness>Checking latest refresh...</span>
          </p>
        </div>
      </header>

      <aside class="app-download-prompt" data-app-download-prompt hidden aria-label="Paddle Today app download">
        <button
          class="app-download-prompt__dismiss"
          type="button"
          data-app-download-dismiss
          aria-label="Dismiss app download prompt"
        >
          &times;
        </button>
        <div class="app-download-prompt__icon" aria-hidden="true">
          <picture>
            <source srcset="/brand/paddle-today-logo-180.webp" type="image/webp" />
            <img src="/brand/paddle-today-logo-transparent-512.png" alt="" width="180" height="180" decoding="async" />
          </picture>
        </div>
        <div class="app-download-prompt__copy">
          <div class="app-download-prompt__headline">
            <span class="app-download-prompt__badge">New</span>
            <strong>Paddle Today mobile apps</strong>
          </div>
          <p>Plan nearby paddles faster from your phone.</p>
        </div>
        <div class="app-download-prompt__actions" aria-label="App download links">
          <a
            class="app-download-prompt__cta"
            href="#"
            data-app-download-link="ios"
            data-analytics-event="Open app download"
            rel="noopener"
            hidden
          >
            iPhone app
          </a>
          <a
            class="app-download-prompt__cta app-download-prompt__cta--secondary"
            href="#"
            data-app-download-link="android"
            data-analytics-event="Open app download"
            rel="noopener"
            hidden
          >
            Android app
          </a>
        </div>
      </aside>

      <main class="main-shell">
        <slot />
      </main>

      <footer class="site-footer">
        <nav class="site-footer__nav" aria-label="Footer">
          <a class="site-footer__link" href="/about/">About</a>
          <a class="site-footer__link" href="/weekend/">Weekend</a>
          <a class="site-footer__link" href="/explore/">Explore</a>
          <a class="site-footer__link" href="/states/">States</a>
          <a class="site-footer__link" href="/guides/">Guides</a>
          <a class="site-footer__link" href="/request-river/">Request a Route</a>
          <a class="site-footer__link" href="/contact/">Contact</a>
          <a class="site-footer__link" href="/privacy/">Privacy</a>
          <a class="site-footer__link" href="/terms/">Terms</a>
        </nav>
        <nav class="site-footer__nav site-footer__nav--states" aria-label="State route pages">
          <span class="site-footer__label">State pages</span>
          {footerStateLinks.map((state) => (
            <a class="site-footer__link" href={`/states/${state.slug}/`}>{state.name}</a>
          ))}
        </nav>
      </footer>

      <section class="site-search-dialog" data-site-search hidden>
        <div class="site-search-dialog__backdrop" data-site-search-close></div>
        <div class="site-search-dialog__panel" id="site-search-panel" role="dialog" aria-modal="true" aria-labelledby="site-search-title">
          <div class="site-search-dialog__head">
            <div class="site-search-dialog__title-wrap">
              <p class="eyebrow">Search</p>
              <h2 class="site-search-dialog__title" id="site-search-title">Find a river or route</h2>
            </div>
            <button class="filter-chip filter-chip--quiet site-search-dialog__dismiss" type="button" data-site-search-dismiss>Close</button>
          </div>

          <label class="site-search-dialog__field">
            <span class="sr-only">Search routes</span>
            <input
              class="site-search-dialog__input"
              type="search"
              placeholder="Search a river, route, town, or area"
              data-site-search-input
              autocomplete="off"
            />
          </label>

          <p class="muted site-search-dialog__hint" data-site-search-hint>Try Snake, Root, Faribault, or Pine City.</p>
          <div class="site-search-dialog__results" data-site-search-results aria-live="polite"></div>
        </div>
      </section>
    </div>
    <script type="application/json" data-site-search-index data-site-search-index-src="/search-index.json"></script>
    <script type="application/json" data-app-download-config set:html={appDownloadConfigJson}></script>
    <script>
      import '../scripts/site-shell.js';
      import '../scripts/analytics.js';
    </script>
  </body>
</html>
