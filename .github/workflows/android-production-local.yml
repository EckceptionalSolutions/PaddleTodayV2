name: Paddle Today Android Production Build

on:
  workflow_dispatch:

concurrency:
  group: android-production-local
  cancel-in-progress: false

jobs:
  build_android_production:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
      GOOGLE_MAPS_ANDROID_API_KEY: ${{ secrets.GOOGLE_MAPS_ANDROID_API_KEY }}
      EXPO_PUBLIC_API_BASE_URL: https://paddletoday.com
      EXPO_PUBLIC_APP_ENV: production
      EAS_LOCAL_BUILD_ARTIFACTS_DIR: ${{ github.workspace }}/apps/mobile/artifacts

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22.12.0'
          cache: npm

      - name: Setup Expo and EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}
          packager: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify release readiness
        run: npm run mobile:release-check

      - name: Typecheck mobile app
        run: npm run mobile:typecheck

      - name: Build Android production app bundle
        working-directory: apps/mobile
        run: eas build --platform android --profile production --local --non-interactive

      - name: Upload Android app bundle
        uses: actions/upload-artifact@v4
        with:
          name: paddletoday-android-production
          path: apps/mobile/artifacts/*
          if-no-files-found: error
