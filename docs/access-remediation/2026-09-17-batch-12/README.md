# Access point remediation batch 12

Reviewed September 17, 2026 under the [step-by-step operator runbook](../../access-point-remediation-runbook.md). The ten physical sites are frozen in [selection.json](selection.json), ranks 141–150, before research. The cached audit ranks only prioritized research; current operator evidence controlled each decision.

One coordinate was corrected: Longfellow Lagoon's stored latitude was about 3.1 miles north of the Minnehaha Creek Watershed District's Longfellow Gardens parking GPS. The official point is an access-area anchor near the required final landing, not a surveyed wet edge. Its route is held until geometry and served maps can be rebuilt and reviewed. Nine other sites remain at their documented public launch, park, or carry-area coordinates; none has a manager-published wet-edge coordinate that supports a more precise replacement. No access site was rejected as unverified in this batch. The source withholding manifest now contains 148 unique route slugs.

## Repeatable steps for a weaker model

1. Read the [operator runbook](../../access-point-remediation-runbook.md), this README, the frozen `selection.json`, and `review.json`. Work ranks 141–150 in order. Never replace a hard site. The audit was cache-only, so queue ranks and water offsets are triage clues, not proof of an incorrect access point.
2. Read `source-metadata.json` first. Use only snapshots with `acceptedAsEvidence: true`. There are 25 accepted snapshots and three failed Minneapolis Park Board retrievals; those failures are not evidence. Do not infer launch rights from a nearby park, road, stream line, or parking lot.
3. For each site, identify what the saved coordinate describes: public site GPS, launch area, park, parking/carry area, or water-entry point. State when a source verifies access but does not publish the wet-edge location. Do not snap a coordinate to a mapped water line just to clear an audit offset.
4. Confirm the selected access matches the route's bank, direction, endpoint role, and lawful path. Keep useful access instructions such as Copper Creek's limited parking, Turtle Creek's hidden trail and grass carry, Mongaup's uneven carry, and Dry Canyon's temporary repair restrictions.
5. Longfellow is the sole coordinate correction. MCWD publishes `44.916125473,-93.215040979` as Longfellow Gardens parking GPS and marks Longfellow Lagoon as the mandatory final landing before Minnehaha Falls. Both the trip-detail endpoint and duplicate access point now use that access-area coordinate. The prior point was about 3.1 miles north. Do not describe the replacement as a surveyed landing coordinate.
6. Longfellow remains held in `src/data/route-access-review-holds.ts` because its canonical geometry and Minnesota map/catalog outputs must be regenerated from the corrected source and inspected. Preserve that hold until the exact landing and falls boundary match, geometry and access audits pass, and stale records are absent from served outputs.
7. Before editing, back up and SHA-256 every changed source file. Backups are under `node_modules/.cache/access-batch-12/before-source/`; hashes are in `before-file-hashes.json`. Do not rewrite the frozen selection. The accepted snapshots live under `node_modules/.cache/access-batch-12/sources/` and their hashes are verified by `verify.mjs`.
8. Regenerate the withholding manifest with `node node_modules/tsx/dist/cli.mjs scripts/generate-withheld-route-slugs.ts`, then run `node docs/access-remediation/2026-09-17-batch-12/verify.mjs`. Follow the runbook's route-data typecheck, fresh full audit, focused tests, geometry generation/audit, overview/catalog generation, and registry generation. Do not treat this batch's syntax verifier as a replacement for the project checks.
9. If the geo package is still missing, keep the Longfellow route held and do not publish or manually claim that the generated map is current. Record the exact failing command. In this checkout the route access registry, Minnesota state geometry bundle, and map overview still contain the route; the registry also still contains the old access coordinate. They require supported regeneration and an absence check before release.
10. Before finishing, verify each frozen rank has one decision, duplicated points use the same role/coordinate, accepted source snapshots and backups match their hashes, failed retrievals remain excluded, and validation describes commands actually run. See `validation.json` for this batch's check results and generated-artifact limitations.

The detailed site-by-site evidence, coordinate roles, route lists, and publication decisions are in [review.json](review.json). This batch fixes one bad source coordinate and clarifies nine verified public access anchors; the Longfellow correction is local source work pending geometry regeneration, not a released map update.
