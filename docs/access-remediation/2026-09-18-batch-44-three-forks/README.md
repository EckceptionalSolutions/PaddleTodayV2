# Three Forks access-anchor audit repair — batch 44

The two routes using BLM's exact Three Forks site coordinate were suspicious because the official access control was not matched to them. BLM places the site in Oregon and publishes **42.545023, -117.167067**, while the route records describe cross-border river reaches under Idaho. The control also used a slightly different name. The audit required an exact state and name match, so it ignored valid BLM evidence.

I added the route endpoint name as an alias and explicitly scoped the Oregon site control to the two route IDs. The matcher now allows this explicit route scope across the state label while leaving unrelated routes out. Both refreshed audits now recognize the exact BLM access-area control and classify the coordinate as **review**, rather than suspicious. No route coordinate moved. The BLM point remains a recreation-site anchor, not a surveyed launch toe; confirm the actual ramp, carry and conditions. BLM's current page confirms the launch and warns that the final road can be steep, rough, and impassable when wet.

There is a third route occurrence named Three Forks that uses a separate point **42.5446, -117.1657**, already within the NHD waterbody and classified review. It remains separate because no source reviewed establishes that it is the exact ramp toe. The audit should not collapse these nearby coordinates based only on the site name.

The frozen selection is in [selection.json](selection.json), the decision and endpoint outcomes are in [review.json](review.json), and source observations are in [source-metadata.json](source-metadata.json). The refreshed route audits are [north-fork-three-forks](targeted-north-fork-three-forks.json) and [three-forks-to-rome](targeted-three-forks-rome.json).

The updated full public audit is **2,750 routes / 6,058 endpoints**: **2,046 ok, 3,834 review, 100 suspicious, 0 failure, 78 unknown**, with 271 source issues and 118 review locations. The suspicious count fell by two from the post-hold batch 43 snapshot because this batch supplied official access-anchor evidence for those two endpoints; they remain review items and no water-entry toe was verified. See [audit-full-cache-only.json](audit-full-cache-only.json). The separate all-inventory audit used for the registry includes held routes: 2,998 routes / 6,641 endpoints, with 154 suspicious, 51 failures, 89 unknown, and 314 source issues; see [audit-full-inventory-cache-only.json](audit-full-inventory-cache-only.json).

The regenerated registry has 4,042 entries, 1,201 repeated entries, **0 conflicts**, 1,196 authoritative access anchors, and 498 authoritative water-entry controls. It now records the exact BLM site anchor for the two scoped route occurrences while retaining a null water-entry coordinate for Three Forks.
