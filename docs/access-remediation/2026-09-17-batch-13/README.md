# Access point remediation batch 13

Reviewed September 17, 2026 under the [step-by-step operator runbook](../../access-point-remediation-runbook.md). The ten physical sites at cached audit ranks 151–160 were frozen in [selection.json](selection.json) before current-source research. The cached ranks prioritize research only; they do not prove that a coordinate is wrong.

This batch corrected no coordinates. Eight sites are supported as public ramp, park, campground, or carry-area anchors, but current manager sources do not provide a more precise surveyed shoreline point. One water objective was reclassified as a navigation waypoint. Barber Flat remains unverified, so its route is now held. The source withholding manifest has 149 unique route slugs. Twenty-three unique routes contain a frozen occurrence in this batch; one new route was withheld.

## Repeatable steps for a weaker model

1. Read the [operator runbook](../../access-point-remediation-runbook.md), this README, `selection.json`, and `review.json`. Work the frozen ranks in order; do not substitute easier sites. The selection was made from the cache-only audit created at `2026-09-17T12:02:46.464Z`.
2. Read `source-metadata.json` before using sources. It contains 24 accepted snapshots and six failed retrievals. Use accepted snapshots only; the failures (five 404s and one 403) are not evidence. The failed pages were an older Iowa DNR Cedar guide, a Forest Service plan PDF, a Nebraska Game and Parks PDF, a USACE status page, and two Arkansas State Parks PDFs. The accepted official interagency Upper Salmon guide was retrieved separately and hashed after the initial source pass.
3. For each site, confirm the named facility with its current manager or operator, then separate access rights from exact coordinates. A public park or campground pin is not automatically the launch edge. Check dam-side, route role, parking, carry, fees, seasonal approach, and current construction/fire notices. Do not snap a point to mapped water to clear an audit warning.
4. Rank 151 (Eureka Bridge): Greene County confirms a public concrete boat ramp; keep the saved point as its access-area anchor. Rank 152 (Interstate Park): the current Mitchell County page and 2024 park map identify the east-end canoe-ramp area; keep the anchor and preserve the Mitchell dam portage/relaunch boundary. The failed legacy DNR link is not evidence either way.
5. Rank 153 (Salmon Island): the interagency guide, IDFG, and the City of Salmon support a public park with two ramps; keep the guide's ramp-area coordinate and preserve the left/north-channel take-out instruction. Rank 154 is the NYSDEC island/head-of-rapids turnaround, not an access site; retain it as `accessPointRole: 'navigation-waypoint'` so the audit does not treat it as a launch or landing.
6. Rank 155 (Barber Flat): the stored point is a Forest Service station/cabin/trailhead, not a verified boat launch. The current Recreation.gov notice says not to visit while the wildfire affects the location. Keep the source record, name its uncertainty, and hold `north-fork-boise-barber-flat-troutdale` until the Forest Service confirms legal river entry, parking/carry, and fire-safe access.
7. Rank 156 (Columbus): keep the hotel / Pawnee Park Trail area point as an access anchor. The City describes hotel parking permission as voluntary; use that private lot only with current permission. Nebraska DOT says public fishing/kayaking access east of the northbound bridge is to remain during construction expected through fall 2027; verify same-day public access and trail routing. Do not label the stored point as the river landing.
8. Rank 157 (Esmond Park): the Town identifies the public park/parking area, and the WRWC guide identifies the launch/take-out below Esmond Dam. Keep the park/carry-area point and preserve the dam portage. Rank 158 (Sullivan): City and Spokane River Water Trail sources confirm public river access; keep the park/parking-area anchor, not a claimed water-entry edge.
9. Rank 159 (Tailwater East): USACE confirms the public ramp at the campground below Coralville Dam; the saved point is the campground/access area. Follow onsite signs and current Corps notices. Rank 160 (U.S. 278): Arkansas Natural Heritage Commission confirms a public developed canoe-launch area in the state park natural area; keep the Highway 278 access-area point and distinguish it from the visitor center and surveyed shoreline.
10. Before editing, hash/back up every touched source file. `before-file-hashes.json` records those hashes; byte-for-byte copies are in `node_modules/.cache/access-batch-13/before-source/`. Source snapshots and their hashes are in the ignored cache under `node_modules/.cache/access-batch-13/sources/`.
11. Regenerate the withholding manifest with `node node_modules/tsx/dist/cli.mjs scripts/generate-withheld-route-slugs.ts`, then run `node docs/access-remediation/2026-09-17-batch-13/verify.mjs`. The verifier checks all frozen sites, complete route lists, accepted-source and backup hashes, the new hold, the navigation-waypoint classification, manifest count, and TypeScript syntax.
12. Follow the runbook's full typecheck, fresh access audit, focused tests, geometry/catalog generation, and registry checks. In this checkout they are blocked by missing `@paddletoday/api-contract`, `@paddletoday/geo`, and `@jridgewell/sourcemap-codec`. Do not claim the generated registry or map is current while the Barber Flat route still appears in served assets. Regenerate and verify the Idaho route geometry/state bundle, overview, registry, and catalog after dependencies return, then confirm the held route is absent and the source hold remains.

The complete evidence, route IDs, roles, decisions, sources, and reopen conditions are in [review.json](review.json). `validation.json` records the exact checks and the generated-artifact limitation. This is source-level remediation; served map/catalog assets were not rebuilt.
