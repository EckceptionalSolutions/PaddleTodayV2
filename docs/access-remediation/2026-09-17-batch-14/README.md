# Access point remediation batch 14

Reviewed September 17, 2026 under the [step-by-step operator runbook](../../access-point-remediation-runbook.md). The ten physical sites at cached access-quality ranks 161–172 were frozen in [selection.json](selection.json) before source research. Ranks 162 and 166 were skipped because all their dependent routes were already withheld; queue order and mapped-water distance prioritize research but do not prove a bad coordinate.

This batch corrected two source coordinates and improved the audit for one non-access navigation objective. Seven sites remain supported access-area anchors. No site was rejected and no route hold was added. The source withholding manifest remains at 149 unique routes. Twenty distinct public route records contain a frozen occurrence; ten route records need fresh geometry because their access coordinate changed (two Missouri endpoints and eight Montana variants).

## Repeatable steps for a weaker model

1. Read the [operator runbook](../../access-point-remediation-runbook.md), this README, `selection.json`, `review.json`, and `source-metadata.json`. Process ranks in frozen order. Use the cache-only audit queue as a research queue, not as proof that a coordinate is wrong.
2. Treat only accepted source snapshots as evidence. The batch has 22 successful, hashed source snapshots. One unrelated Village of Catskill parks-rules PDF returned 404 and is excluded; the Village’s 2025 DRI application and NYSDEC access directory succeeded and support the Catskill decision.
3. Separate site identity, public access, and coordinate precision. A manager’s campground or park pin can validate an access area without identifying the exact ramp toe. Do not snap a point to mapped water. Preserve parking, carry, dam, fee, closure, easement, and seasonal caveats.
4. Rank 161: the Miami River point is a turnaround at a beaver-dam constraint, not a launch or landing. Retain its route use but set `accessPointRole: 'navigation-waypoint'`. Rank 163: DNREC confirms the Blackbird Creek Reserve public canoe/kayak launch; retain the ramp-area anchor and dawn-to-dusk/hunting closure notes. Rank 164: NYSDEC lists the Tioga River Lindley Road hand launch and parking; keep the bridge-area anchor because the directory gives no surveyed GPS.
5. Rank 165: LCRA confirms FM 973/Del Valle Bridge as a TxDOT-managed access with limited roadside parking and uneven carry; keep the rounded bridge-area anchor. Rank 167: replace the Meramec River Stop Store pin with the distinct Missouri State Parks Boat Launch feature coordinate `38.203259, -91.09888`; the store feature has a different pin. Keep it an access-area anchor, not a surveyed wet edge.
6. Rank 168: Story County confirms public Soper’s Mill #235 and separate north and south access areas. Retain the water-trail-area point and direct this run to the south access; preserve the constructed-riffle scouting note. Rank 169: American Whitewater describes the Spring Creek campground ramp and the Forest Service road map marks the campground turn; keep it as an optional campground/ramp-area anchor, with a same-day road and campground check.
7. Rank 170: Circle Pines confirms the E. Golden Lake Road canoe launch and parking, while Anoka County’s water-trail map labels East Golden Lake Lane; keep the city access-area anchor. Rank 171: NYSDEC identifies Kiwanis Park as a municipal hand launch; the Village’s 2025 application says public access currently uses two gravel paths under an easement and proposes an ADA launch on a 2026 timeline. Keep existing caveats and do not describe the planned dock as open. Rank 172: Montana FWP publishes Whitaker Bridge FAS at `46.94277, -113.59983` and lists a concrete ramp; update every variant copy and call it a public access-area coordinate, not an exact water-entry edge.
8. Before edits, exact byte copies and SHA-256 hashes were recorded in `before-file-hashes.json`; prior registry/control matches and file hashes are captured in `registry-control-snapshot.json`. Backups are under `node_modules/.cache/access-batch-14/before-source/`. Coordinate source pages and PDFs are hashed in `source-metadata.json` under the ignored cache.
9. Two official access-area controls were added to `src/data/route-access-official-map-controls.json` for the named Missouri State Parks boat-launch feature and Montana FWP site. These allow a future access-quality audit to recognize documented agency coordinates without claiming they equal exact water entry.
10. Run `node node_modules/tsx/dist/cli.mjs scripts/generate-withheld-route-slugs.ts`, then `node docs/access-remediation/2026-09-17-batch-14/verify.mjs`. Next run the route typecheck, fresh access audit, focused tests, and route geometry/catalog/overview generation from the main runbook. Do not run `--reuse-existing` until every changed public route has fresh geometry. In this checkout required packages are missing; `validation.json` records the exact failures. Current generated registry, map overview, and geometry assets are therefore not release-ready.

The complete per-site decisions, source URLs, original and replacement coordinates, route IDs, and roles are in [review.json](review.json). This is a source-level fix; served artifacts remain stale until regeneration succeeds.
