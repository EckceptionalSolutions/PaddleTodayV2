// Native builds intentionally do nothing. The web implementation lives in
// suppress-web-font-timeout.web.ts so Android/iOS release bundles do not load
// browser-only error handling during Expo Router startup.
